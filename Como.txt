
ionic start

ionic g component components/header
ionic g page pages/detalle
ionic generate pipe pipes/priceFormat
Compartir pipes entre modulos
1) ionic g m sharedpipes
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { PriceFormatPipe } from '../pipes/price-format.pipe';
import { TypeViviendaPipe } from '../pipes/type-vivienda.pipe';


@NgModule({
  declarations: [PriceFormatPipe, TypeViviendaPipe ],
  imports: [
    CommonModule
  ],
  exports: [PriceFormatPipe, TypeViviendaPipe ]
})
export class SharedpipesModule { }

2) en los modulos donde se necesite el pipe

en el module.ts del componente o pages
import { SharedpipesModule } from '../../sharedpipes/sharedpipes.module';
@NgModule({
  imports: [
    ...
    ...
    ...
    SharedpipesModule
  ],


ionic g module components
me genera components/components.module.ts
añadir
@NgModule({
  declarations: [HeaderComponent],
  imports: [
    CommonModule,
    IonicModule
  ],
  exports:[
    HeaderComponent
  ]
})

En app/app.module.ts
 import { ComponentsModule } from './components/components.module'; y añadir en imports  ComponentsModule

En header.component.html poner
<ion-header>
  <ion-toolbar>
    <ion-title>Header</ion-title>
  </ion-toolbar>
</ion-header>
y dejar el app.component.html
<ion-app>
  <ion-router-outlet></ion-router-outlet>
</ion-app>

En header.component.ts poner
export class HeaderComponent implements OnInit {
  @Input() titulo!: string;
  constructor() { }

  ngOnInit() {}

}

Y en las pages ejemplo home.page.html
<app-header titulo="home2"></app-header>
<ion-content>

</ion-content>

Hacer lo mismo para el footer

Correr en emulador o dispositivo
ionic capacitor run android
o
ionic serve --lab    -para ver en diferentes dispositivos

para debug
ionic capacitor run android -l --host=192.168.1.140

para instalar en el movil
ionic capacitor run android 

Crear android
ionic capacitor build android
ionic capacitor copy android

Crear ios
ionic capacitor build ios
ionic capacitor copy ios


Cambiar icono del movil

npm install capacitor-resources -g

crear carpeta resources en la raiz y copiar icon.png y splash.png

capacitor-resources

Abrir el proyecto Android en Android Studio
ionic capacitor open android
cartpeta res boton izquierdo new asset Image


ionic cap sync




Instalaciones para Mac

Install brew

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

brew uninstall --force git
Then cleanup any older versions and clear the brew cache
brew cleanup -s git
Remove any dead symlinks
brew cleanup --prune-prefix
Then try reinstalling git
brew install git

brew install cocoapods


ionic capacitor build ios
ionic capacitor open ios

Si falla
sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

npx cap sync

