import { TypeViviendaPipe } from './type-vivienda.pipe';

describe('TypeViviendaPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeViviendaPipe();
    expect(pipe).toBeTruthy();
  });
});
