import { RemoveWordsPipe } from './remove-words.pipe';

describe('RemoveWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
